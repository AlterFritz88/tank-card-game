# Headquarters avatars

Put transparent headquarters avatar images here.

Naming convention:

- `training_unit.png`
- `trainingslager.png`
- `training_camp.png`
- `first_panzer_division.png`

The file name must match the headquarters id. Supported formats: `png`, `jpg`, `jpeg`, `webp`.
